---
import AvatarList from "./AvatarList.astro";
type Props = {
  path: string;
};
const { path } = Astro.props;
---

<footer>
  <!-- <AvatarList path={path} /> -->
  <h3>This is a provisional footer :)</h3>
</footer>

<style>
  footer {
    margin-top: auto;
    padding: 2rem;
    border-top: 3px solid var(--theme-divider);
  }
</style>
